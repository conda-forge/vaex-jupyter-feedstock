{% set name = "vaex-jupyter" %}
{% set version = "0.4.0" %}
{% set sha256 = "e3c6a21a347f436dcf16ec14f7248215667f614cb2bcf6cdad153708ced4114f" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python >=3.6
    - setuptools
    - pip
  run:
    - python
    - vaex-core >=1.0.0,<2
    - vaex-viz
    - bqplot >=0.10.1
    - ipyvolume >=0.4
    - ipyleaflet
    - ipympl
    - ipyvuetify

test:
  imports:
    - vaex.jupyter

about:
  home: https://www.github.com/maartenbreddels/vaex
  doc_url: https://docs.vaex.io/
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: Jupyter notebook and Jupyter lab support for vaex

extra:
  recipe-maintainers:
    - maartenbreddels
